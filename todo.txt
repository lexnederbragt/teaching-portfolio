* consider adding ` pandoc -s --toc src/main.md -o pub/mappe.md` for github
  - also update README
* add `ghp-import -n -p -f docs/_build/html/` for gh-pages
* footnotes in jupyterbook have same formatting as citations.
* how to remove bibliography created by DocOnce for word output, while keeping it for jupyterbook?
* fix missing URLs in bibliography
* produce mappe.pdf ; so far works but citations become [?]
```
cd src && \
doconce format pdflatex main.do.txt \
    --latex_code_style=vrb \
    --allow_refs_to_external_docs \
    --xelatex && \
xelatex main && \
xelatex main && \
makeindex main && \
bibtex main && xelatex \
main
```
